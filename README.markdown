Python `requirements.txt` generator

#### API

generate `requirements.txt` with [REST API](https://requires42.com/api/)

#### CLI

super fast <a class="muted-link" href="/cli/">shell cli</a></p>
```bash
$ requires42 . > requirements.txt
```

#### Customization

create custom replacements. example:

`configurations` > `django-configurations`
